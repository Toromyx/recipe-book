name: CI

on: [push]

jobs:

  ci:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/toromyx/recipe-book:github-actions-test
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v3
      - run: npm ci
      - run: npm run audit
      - run: npm run build
      - run: npm run check
      - run: xvfb npm run test
